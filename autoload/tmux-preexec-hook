# local a=$(tmux display-message -p '#W')
command=(${=1})
case ${command[1]} in
  ssh )
      tmux rename-window "${command[2]##-}"
    ;;
  mutt )
      tmux rename-window "MUTT"
    ;;
esac
# tmux rename-window "$a"

# vim: ft=zsh
