function prompt_sencer_setup {
  prompt_opts=(cr percent subst)
  # Define prompts.
  export PROMPT="%F{73}%h:%F{174}%C%f " 

  if [[ -n $TMUX ]]; then
    export RPROMPT="%F{73}[\${PWD/${HOME//\//\\/}/~}]%f"
  else
    export RPROMPT="%F{73}\$(tmux-settings namaz)[\${PWD/${HOME//\//\\/}/~}]%f"
  fi
}

prompt_sencer_setup "$@"

# vim: ft=zsh
